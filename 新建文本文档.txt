# 核心
pyyaml          # 配置加载
polars          # 高性能 DataFrame
pandas          # 兼容模式

# GUI（可选）
qtpy            # PyQt/PySide 抽象层

# 日志/网络（可选）
requests        # 下载文件
langdetect      # 语言识别

# CLI（可选）
click           # 命令行工具